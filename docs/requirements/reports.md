# レポート（グラフ）要件

## 対応デザイン

- `docs/designs/reports.png`
- `docs/designs/reports.svg`

## 目的

支出の傾向をグラフで把握し、カテゴリ別・月別の分析をできるようにする。

## 表示項目

- 支出合計
- 1日あたりの平均支出
- 最大の支出カテゴリ
- 最小の支出カテゴリ
- カテゴリ別支出割合
- カテゴリ別支出推移
- 月別支出推移

## 操作

- 集計単位を切り替えられる。
- 対象年月を選択できる。
- レポートをエクスポートできる。

## 業務ルール

- レポートは支出明細を対象にする。
- 対象期間内の明細をカテゴリ別に集計する。
- 1日あたりの平均支出は、対象期間の支出合計を対象日数で割る。
- 最大の支出カテゴリは対象期間で支出合計が最も大きいカテゴリとする。
- 最小の支出カテゴリは対象期間で支出合計が最も小さいカテゴリとする。
- 月別支出推移は月単位で支出合計を集計する。

## 例外・空状態

- 対象期間に明細がない場合は、集計値を0として空状態を表示する。
- グラフ描画に必要なデータがない場合は、グラフ領域に空状態を表示する。
- エクスポートに失敗した場合は、再実行できるエラー表示にする。

## 確認事項

- レポートの集計単位は月別以外に週別・年別も必要か。
- レポートのエクスポート形式はCSV、PDF、画像のどれにするか。
