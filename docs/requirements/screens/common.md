# 画面共通要件

この文書は、複数画面に共通する表示前提やUI要件をまとめる。

## 共通表示ルール

- 初期タイムゾーンは `Asia/Tokyo` を基準にする。
- 日付の初期表示形式は `YYYY/MM/DD` を使う。
- 金額の数値入力は `inputMode="numeric"` を使い、増減用ステッパーは表示せず直接入力を基本とする。
- `収入` は緑系、`支出` は赤系、`収支` は黄系を共通テーマカラーとして扱い、金額表示、アイコン、補助UIを含めて同じ意味の表示で画面ごとに別の色へぶらさない。
